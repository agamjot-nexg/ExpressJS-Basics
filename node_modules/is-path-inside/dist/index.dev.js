'use strict';

var path = require('path');

module.exports = function (childPath, parentPath) {
  var relation = path.relative(parentPath, childPath);
  return Boolean(relation && relation !== '..' && !relation.startsWith("..".concat(path.sep)) && relation !== path.resolve(childPath));
};